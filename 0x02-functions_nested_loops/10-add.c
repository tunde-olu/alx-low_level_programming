#include "main.h"

/**
 * add - adds two integers
 * @a: one of the two number to add
 * @b: the argument of the other number to add
 * Return: The addition of a and b
 */

int add(int a, int b)
{
	result = a + b;

	return (result);
}
